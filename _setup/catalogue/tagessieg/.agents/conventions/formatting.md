# Formatting Conventions

* You MUST respect the formatting rules defined in `.editorconfig`.
* Specifically, use **2 spaces** for indentation, as mandated by `.editorconfig`.
* Ensure `insert_final_newline` is set to `true` (always end files with a newline).
* Trim trailing whitespace.
